#!/usr/bin/expect -f

set timeout 30
set password "00203549Rk.."

spawn ssh -o StrictHostKeyChecking=no root@31.97.78.86
expect {
    "password:" { send "$password\r" }
    timeout { puts "Timeout"; exit 1 }
}

expect "#"
send "rm /etc/nginx/sites-enabled/syncarch.xyz\r"
expect "#"

send "nginx -t\r"
expect "#"

send "systemctl restart nginx\r"
expect "#"

send "echo '=== CONFIGS CLEANED AND NGINX RESTARTED ==='\r"
expect "#"

send "exit\r"
expect eof
